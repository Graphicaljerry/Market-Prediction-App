node_modules/
tools/preview.png
tools/*.png
# local preview tooling only (npm install puppeteer) — kept out of the repo root
# so Cloudflare's git-connected Worker build doesn't try to install it
package.json
package-lock.json

# macOS clutter
.DS_Store

# Local Claude Code state — commit only the shared project settings + the skills.
# (history, caches, backups, plugins, session/telemetry data stay local.)
.claude/*
!.claude/settings.json
!.claude/skills/
